<div class="news">
    {% for post in news %}
        <div class="post">
            <h2 class="post-title">{{ post.title }}</h2>
            <span class="post-title">{{ post.createdAt | date }}</span>
            <p>{{ post.content | raw }}</p>
        </div>
    {% endfor %}
</div>
